{
  "name": "laterbox-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd web && npm run build && cd .. && node --env-file=.env --watch index.js",
    "start": "node index.js",
    "build": "cd web && npm install --include=dev && npm run build"
  },
  "dependencies": {
    "axios": "^1.13.4",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.0.0",
    "multer": "^1.4.5-lts.1",
    "twilio": "^5.12.0",
    "uuid": "^10.0.0",
    "ws": "^8.19.0"
  }
}
